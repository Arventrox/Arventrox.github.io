export default class Player {
	playerName: string;
	// PlayerRole: string;
	// playerChampion: string;

	constructor(playersName: string, playerRole: string, playerChampion: string) {
		this.playerName = playersName;
		// This.playerRole = playerRole;
	}
}

